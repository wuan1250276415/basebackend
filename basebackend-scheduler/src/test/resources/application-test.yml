spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 

camunda:
  bpm:
    database:
      type: h2
    history-level: full
    metrics:
      enabled: false

logging:
  level:
    com.basebackend.scheduler: DEBUG
    org.camunda: WARN
