/**
 * 工作流声明式注解扩展点（预留）。
 * <p>
 * 计划提供注解驱动的工作流集成能力，例如：
 * <ul>
 *   <li>{@code @WorkflowStart} — 标记方法调用时自动启动流程实例</li>
 *   <li>{@code @WorkflowApproval} — 标记审批入口方法</li>
 * </ul>
 */
package com.basebackend.workflow.annotation;
