<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.user.mapper.SysApplicationMapper">

    <resultMap id="BaseResultMap" type="com.basebackend.user.entity.SysApplication">
        <id column="id" property="id" />
        <result column="app_name" property="appName" />
        <result column="app_code" property="appCode" />
        <result column="app_type" property="appType" />
        <result column="app_icon" property="appIcon" />
        <result column="app_url" property="appUrl" />
        <result column="status" property="status" />
        <result column="order_num" property="orderNum" />
        <result column="remark" property="remark" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <!-- 查询所有启用的应用 -->
    <select id="selectEnabledApplications" resultMap="BaseResultMap">
        SELECT * FROM sys_application
        WHERE status = 1 AND deleted = 0
        ORDER BY order_num ASC, create_time ASC
    </select>

    <!-- 根据应用编码查询应用 -->
    <select id="selectByAppCode" resultMap="BaseResultMap">
        SELECT * FROM sys_application
        WHERE app_code = #{appCode} AND deleted = 0
        LIMIT 1
    </select>

</mapper>
