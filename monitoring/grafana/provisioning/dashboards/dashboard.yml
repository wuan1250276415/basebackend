# Grafana 仪表板自动加载配置

apiVersion: 1

providers:
  - name: 'BaseBackend DAG Engine'
    orgId: 1
    folder: 'BaseBackend'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
