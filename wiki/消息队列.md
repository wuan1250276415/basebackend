[< 返回首页](Home) | [< 上一页: 缓存模块详解](缓存模块详解)

---

# 消息队列

---

## 概述

`basebackend-messaging` 模块集成 RocketMQ 5.2.0，提供消息发送/消费、事务消息、幂等消费、死信队列等能力。

---

## 架构

```
Producer (微服务)
    │
    ▼
RocketMQ NameServer (9876)
    │
    ▼
RocketMQ Broker (10911)
    │
    ├── 普通消息 ──→ Consumer
    ├── 事务消息 ──→ Consumer (最终一致性)
    ├── 延迟消息 ──→ Consumer (定时触发)
    └── 死信消息 ──→ DLQ Consumer (兜底处理)
```

---

## 基础配置

```yaml
rocketmq:
  name-server: 192.168.66.126:9876
  producer:
    group: basebackend-producer-group
    send-message-timeout: 3000
    compress-message-body-threshold: 4096
    max-message-size: 4194304          # 4MB
    retry-times-when-send-failed: 2
    retry-times-when-send-async-failed: 2
  consumer:
    group: basebackend-consumer-group
    consume-timeout: 15

messaging:
  rocketmq:
    enabled: true
    default-topic: basebackend-topic
    topic-prefix: basebackend.
  retry:
    enabled: true
    max-attempts: 16
  dead-letter:
    enabled: true
    topic: basebackend-dlq-topic
    consumer-group: basebackend-dlq-consumer-group
  transaction:
    enabled: true
    table-name: sys_message_log
    check-interval: 60
    timeout: 30
  idempotency:
    enabled: true
    expire-time: 3600
    key-prefix: "msg:idempotent:"
```

---

## 消息发送

### 同步发送

```java
@Autowired
private RocketMQTemplate rocketMQTemplate;

// 同步发送
public void sendSync(String topic, Object payload) {
    rocketMQTemplate.syncSend(topic, MessageBuilder.withPayload(payload).build());
}
```

### 异步发送

```java
// 异步发送
public void sendAsync(String topic, Object payload) {
    rocketMQTemplate.asyncSend(topic, MessageBuilder.withPayload(payload).build(),
        new SendCallback() {
            @Override
            public void onSuccess(SendResult result) {
                log.info("消息发送成功: msgId={}", result.getMsgId());
            }

            @Override
            public void onException(Throwable throwable) {
                log.error("消息发送失败", throwable);
            }
        });
}
```

### 单向发送（不关心结果）

```java
// 单向发送 - 日志类消息
public void sendOneWay(String topic, Object payload) {
    rocketMQTemplate.sendOneWay(topic, MessageBuilder.withPayload(payload).build());
}
```

---

## 消息消费

```java
@Component
@RocketMQMessageListener(
    topic = "basebackend.user-created",
    consumerGroup = "user-created-consumer-group"
)
public class UserCreatedConsumer implements RocketMQListener<UserCreatedEvent> {

    @Override
    public void onMessage(UserCreatedEvent event) {
        log.info("收到用户创建事件: userId={}", event.getUserId());
        // 处理业务逻辑：发送欢迎邮件、初始化配置等
    }
}
```

---

## 事务消息

用于跨服务操作的最终一致性保障。

### 流程

```
1. Producer 发送半消息（Half Message）
2. Broker 存储半消息，返回确认
3. Producer 执行本地事务
4. 本地事务成功 → Commit 半消息 → Consumer 可消费
   本地事务失败 → Rollback 半消息 → 消息丢弃
5. 超时未确认 → Broker 回查本地事务状态
```

### 使用示例

```java
// 发送事务消息
public void createOrderWithTransaction(OrderCreateDTO dto) {
    rocketMQTemplate.sendMessageInTransaction(
        "order-tx-group",
        "basebackend.order-created",
        MessageBuilder.withPayload(dto).build(),
        dto  // 透传给本地事务执行器
    );
}

// 本地事务执行器
@RocketMQTransactionListener(txProducerGroup = "order-tx-group")
public class OrderTransactionListener implements RocketMQLocalTransactionListener {

    @Override
    public RocketMQLocalTransactionState executeLocalTransaction(Message msg, Object arg) {
        try {
            OrderCreateDTO dto = (OrderCreateDTO) arg;
            orderService.createLocal(dto);
            return RocketMQLocalTransactionState.COMMIT;
        } catch (Exception e) {
            return RocketMQLocalTransactionState.ROLLBACK;
        }
    }

    @Override
    public RocketMQLocalTransactionState checkLocalTransaction(Message msg) {
        // 回查本地事务状态
        String orderId = msg.getHeaders().get("orderId", String.class);
        boolean exists = orderService.exists(orderId);
        return exists ? RocketMQLocalTransactionState.COMMIT
                     : RocketMQLocalTransactionState.ROLLBACK;
    }
}
```

---

## 幂等消费

防止消息重复消费：

```
消息到达 → 提取 Message ID
              │
              ▼
       Redis 检查是否已消费
       Key: msg:idempotent:{msgId}
              │
        ┌─────┼─────┐
      已存在       不存在
        │           │
    跳过消费     处理消息
                    │
               标记为已消费
               TTL: 3600s
```

---

## 死信队列

消费失败达到最大重试次数后，消息进入死信队列：

```java
@Component
@RocketMQMessageListener(
    topic = "basebackend-dlq-topic",
    consumerGroup = "basebackend-dlq-consumer-group"
)
public class DeadLetterConsumer implements RocketMQListener<MessageExt> {

    @Override
    public void onMessage(MessageExt msg) {
        log.error("死信消息: topic={}, msgId={}, body={}",
            msg.getTopic(), msg.getMsgId(), new String(msg.getBody()));
        // 告警通知 / 人工处理 / 存入数据库
    }
}
```

---

| [< 上一页: 缓存模块详解](缓存模块详解) | [下一页: API 网关 >](API网关) |
|---|---|
