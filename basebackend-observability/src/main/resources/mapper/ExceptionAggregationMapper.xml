<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.observability.mapper.ExceptionAggregationMapper">

    <select id="selectTopExceptions" resultType="com.basebackend.observability.entity.ExceptionAggregation">
        SELECT * FROM exception_aggregation
        WHERE last_seen &gt;= #{startTime}
        ORDER BY occurrence_count DESC
        LIMIT #{limit}
    </select>

    <select id="selectByService" resultType="com.basebackend.observability.entity.ExceptionAggregation">
        SELECT * FROM exception_aggregation
        WHERE service_name = #{serviceName}
        ORDER BY occurrence_count DESC
    </select>

    <update id="incrementOccurrence">
        UPDATE exception_aggregation
        SET occurrence_count = occurrence_count + 1,
            last_seen = NOW()
        WHERE stack_trace_hash = #{stackTraceHash}
    </update>

</mapper>
