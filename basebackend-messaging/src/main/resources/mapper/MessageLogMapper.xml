<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.messaging.mapper.MessageLogMapper">

    <select id="selectTimeoutMessages" resultType="com.basebackend.messaging.entity.MessageLogEntity">
        SELECT id, message_id, mq_message_id, topic, tag, payload, headers,
               send_time, delay_millis, retry_count, max_retries,
               partition_key, status, error_message, create_time, update_time
        FROM sys_message_log
        WHERE status IN
        <foreach collection="statuses" item="s" open="(" separator="," close=")">
            #{s}
        </foreach>
          AND create_time &lt; DATE_SUB(NOW(), INTERVAL #{timeoutMinutes} MINUTE)
          AND retry_count &lt; max_retries
        LIMIT #{limit}
    </select>

</mapper>
