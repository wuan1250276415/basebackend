# Demo-API 专用配置
server:
  port: 8081

spring:
  # 数据源配置（可选，如果需要测试数据库功能）
  datasource:
    url: jdbc:mysql://localhost:3306/basebackend_demo?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${DEMO_DB_USERNAME:root}
    password: ${DEMO_DB_PASSWORD:root123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: ${DEMO_REDIS_PASSWORD:redis2025}

# RocketMQ配置
rocketmq:
  name-server: localhost:9876
  producer:
    group: demo-producer-group
    send-message-timeout: 3000
    retry-times-when-send-failed: 2

# 文件上传配置
file:
  upload:
    path: /tmp/basebackend/uploads
    max-size: 10485760  # 10MB
